#include <stdio.h>

main()
{
	int a = 80;
	float f = 45.3;
	float suma = (float)a + f;
	printf("%.2f", suma);
	//printf("entero %i\n flotante %.2f\n double %.3f\n caracter %c\n",a, (float)a, (double)a,(char)a);
	return 0;
}
